Usage:
  php morphine [config|generate|execute|install|update|test|--help]

Options:
  config            # Set or get configuration options for morphine
  generate          # Generates packages, modules, workflows, models, etc
  execute           # Executes a workflow in a package
  install           # Installs a package or module
  update            # Checks the package server to see if there are updates
  test              # Runs the test suite for a package
  --help            # Displays this help file